rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Invoice number counter (settings/invoiceCounter)
    match /settings/{docId} {
      allow read, write: if true;
    }

    // Saved invoices
    match /invoices/{invoiceId} {
      allow read, write: if true;
    }
  }
}
